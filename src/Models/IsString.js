"use strict";
/**
 * Tests if the value property is a string type in javascript.
 * Utility function for isString method.
 * @param {any} value 
 * @returns {boolean}
 **/
export const IsString = (value) => typeof value === 'string'
