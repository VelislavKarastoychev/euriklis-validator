"use strict";
/**
 * Tests if a value is boolean type in javascript.
 * Utility function for the isBoolean method.
 * @param {any} value
 * @returns {boolean}
 **/
export const IsBoolean = (value) => typeof value === 'boolean'
