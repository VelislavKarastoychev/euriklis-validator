"use strict";
/**
 * @external {validator}
 * @link {../validator.js}
 * A callback function for every of the
 * elements of a set.
 * @callback Callback
 * @param {validator} element
 * @param {number} index
 * @returns {boolean}
 **/
/**
 * @param {any} value
 * @param {Callback} callback 
 **/
export const ForAllSetEdition = (value, callback) => {
  let i, j, question = false;
  return question;
}
