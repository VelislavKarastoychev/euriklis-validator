"use strict";
/**
 * Tests if the value parameter is Array type.
 * Utility function for the isArray method.
 * @param {Array} value
 * @returns {boolean}
 */
export const IsArray = (value) => value instanceof Array;
